# Copy this file to `.act.secrets` and populate with real credentials before running `act`.
# Never commit `.act.secrets` to version control.

MYTHOSMUD_PAT=ghp_your_personal_access_token

# Optional: add any additional secrets referenced in workflows (e.g., NPM_TOKEN, GH_TOKEN).
