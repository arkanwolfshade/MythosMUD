# MythosMUD Client Environment Variables
# Copy this file to .env and update values as needed

# API Base URL
# - Leave empty or unset for production (uses relative URLs - same origin)
# - Set to full URL for development (e.g., http://localhost:54731)
# - Set to full URL for staging/production if API is on different domain
VITE_API_URL=http://localhost:54731

# Log Level (optional)
# Controls client-side logging verbosity
# Options: DEBUG, INFO, WARN, ERROR
# Default: DEBUG in development, WARN in production
# VITE_LOG_LEVEL=DEBUG
