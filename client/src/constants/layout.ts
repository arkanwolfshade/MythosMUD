/**
 * Layout constants for stacking and positioning.
 * Use these instead of magic numbers so z-order is manageable across the app.
 */

/** Maximum stacking layer for modals, popouts, and full-screen overlays (above all panels). */
export const Z_INDEX_OVERLAY_TOP = 2147483647;
