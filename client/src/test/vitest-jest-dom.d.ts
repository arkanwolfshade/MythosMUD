/**
 * Augments Vitest `expect` with @testing-library/jest-dom matchers for TypeScript.
 * Runtime registration is in setup.ts (`import '@testing-library/jest-dom/vitest'`).
 */
/// <reference types="@testing-library/jest-dom/vitest" />
