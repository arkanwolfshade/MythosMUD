# Legacy Schema Files (Removed)

The SQL files that were in this directory have been **removed**.

Schema is maintained only via the single, schema-agnostic **`db/schema.sql`** (#811). See
`db/README.md` and `db/LEGACY_FILES.md`.
