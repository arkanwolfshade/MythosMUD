# Legacy Schema Files (Removed)

The SQL files that were in this directory have been **removed**.

Schema is maintained only via **authoritative DDL** in `db/`:

- `db/mythos_dev_ddl.sql`
- `db/mythos_unit_ddl.sql`
- `db/mythos_e2e_ddl.sql`

Use the DDL that matches your database name. See `db/README.md` and `db/LEGACY_FILES.md`.
