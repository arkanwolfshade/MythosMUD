{
	"name": "mythosmud-static-data",
	"private": true,
	"type": "module",
	"version": "0.1.0",
	"description": "Static data validator and SQL generator for MythosMUD",
	"scripts": {
		"validate": "node ./validate.mjs",
		"generate-sql": "node ./generate_sql.mjs",
		"build": "npm run validate && npm run generate-sql"
	},
	"dependencies": {
		"ajv": "^8.12.0",
		"ajv-formats": "^2.1.1",
		"uuid": "^9.0.1"
	}
}
