# Command tests moved

Unit tests for commands have been moved to the standard test tree.

- **Logout command tests:** `server/tests/unit/commands/test_logout_command.py`

This directory is kept for reference. New command tests should be added under
`server/tests/unit/commands/`.
